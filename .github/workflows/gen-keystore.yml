name: Generate signing keystore (one-off)

on:
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17

      - name: Generate keystore
        env:
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          # PKCS12 (keytool's default since JDK 9, regardless of the .jks
          # extension) requires the key password to equal the store
          # password -- pass the same value for both explicitly so there's
          # no silent keytool override to get out of sync with later.
          keytool -genkeypair -v \
            -keystore release.jks \
            -alias "$KEY_ALIAS" \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass "$KEYSTORE_PASSWORD" \
            -keypass "$KEYSTORE_PASSWORD" \
            -dname "CN=Courier, OU=Courier, O=Courier, L=, ST=, C=US"

      - uses: actions/upload-artifact@v4
        with:
          name: release-keystore
          path: release.jks
          retention-days: 1
